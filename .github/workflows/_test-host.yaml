name: Test (host)

# Reusable workflow that runs the host-side test pillar — bun tests,
# Django unit tests on the host runner, and Docker-based integration
# tests. Kept separate from the build pillar so devs see red within
# ~1-2 min instead of waiting for a Docker image build.

on:
  workflow_call:
    secrets:
      CODECOV_TOKEN:
        # Forks on PR don't inherit secrets; the codecov upload is
        # guarded by a presence check below, so this is `required:
        # false` for the workflow_call contract.
        required: false

permissions:
  contents: read

jobs:
  typescript-tests:
    name: TypeScript tests (host)
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Setup Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2
        with:
          bun-version: 1.3.13

      - name: Install JS dependencies
        run: bun install --frozen-lockfile

      - name: Run bun test
        run: bun test

  python-unit-tests:
    name: Python unit tests (host)
    runs-on: ubuntu-24.04
    # Lift CODECOV_TOKEN to the job env so the codecov-upload step's
    # `if: env.CODECOV_TOKEN != ''` guard can actually see it. Step
    # `env:` is invisible to step `if:`, and the `secrets` context
    # isn't usable in `if:` either, so the job-level env is the
    # cleanest way to gate gracefully on fork PRs without secrets.
    env:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      # Native deps required to build cec / netifaces from source —
      # mirrors the apt set in tools/image_builder/__main__.py.
      - name: Install native build deps
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential \
            cec-utils \
            libcec-dev \
            libffi-dev \
            libsystemd-dev

      - name: Set up Python + uv (test deps)
        uses: ./.github/actions/setup-python-uv
        with:
          group: test

      # AnthiasSettings reads ~/.anthias/anthias.conf at import time;
      # use the canonical file so unit tests see realistic defaults
      # rather than the three-empty-section minimal that mypy gets.
      # `~/anthias_assets/` is the default `assetdir` resolved from
      # $HOME — tearDown in api/tests/test_v1_endpoints.py iterates it
      # and FileNotFoundErrors if missing. Container images get this
      # path for free; the host runner has to materialize it.
      - name: Seed ~/.anthias/anthias.conf and ~/anthias_assets/
        run: |
          mkdir -p ~/.anthias ~/anthias_assets
          cp ansible/roles/anthias/files/anthias.conf ~/.anthias/anthias.conf

      - name: Run Django unit tests
        run: |
          uv run ./manage.py test \
            --noinput --parallel --exclude-tag=integration

      # Forks on PR don't get secrets — skip codecov when the token
      # isn't available rather than uploading an empty report. The
      # token is set at job-level env above so this `if:` can see it
      # (step `env:` and the `secrets` context are both invisible to
      # step `if:` evaluation).
      - name: Upload coverage reports to Codecov
        if: env.CODECOV_TOKEN != ''
        uses: codecov/codecov-action@57e3a136b779b570ffcdbf80b3bdc90e7fab3de2 # v6

  python-integration-tests:
    name: Python integration tests (Docker)
    runs-on: ubuntu-24.04
    env:
      COMPOSE_FILE: docker-compose.test.yml
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Set up Python + uv (image builder)
        uses: ./.github/actions/setup-python-uv
        with:
          group: docker-image-builder

      - name: Render test Dockerfiles
        run: |
          uv run python -m tools.image_builder \
            --dockerfiles-only \
            --disable-cache-mounts \
            --service celery \
            --service redis \
            --service test

      - name: Start the test stack
        run: docker compose up -d --build

      - name: Prepare integration test environment
        run: |
          docker compose exec anthias-test \
            bash ./bin/prepare_test_environment.sh -s

      - name: Run Django integration tests
        uses: nick-fields/retry@ad984534de44a9489a53aefd81eb77f87c70dc60 # v4
        with:
          timeout_minutes: 5
          max_attempts: 5
          command: |
            docker compose exec anthias-test \
              ./manage.py test --noinput --tag=integration

      - name: Stop the test stack
        if: always()
        run: docker compose down
