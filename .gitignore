*.py[co]
.idea/
.qt5-toolchain-build/

# Packages
*.egg
*.egg-info
dist
build
eggs
parts
var
sdist
develop-eggs
.installed.cfg

# Installer logs
pip-log.txt

# Unit test / coverage reports
.coverage
.tox

#Translations
*.mo

#Mr Developer
.mr.developer.cfg

# VIM
*.swp

.DS_Store
.fseventsd/*
.Spotlight-V100/*
.sass-cache/*
venv
node_modules
*.retry
.env
.balena/balena.yml

# Ignore all non-template based files
docker/Dockerfile.base
docker/Dockerfile.nginx
docker/Dockerfile.server
docker/Dockerfile.websocket
docker/Dockerfile.redis
docker/Dockerfile.viewer
docker/Dockerfile.test
docker-compose.yml

balena-deploy/
db.sqlite3

# Local pytest SQLite database (created by
# src/anthias_server/django_project/settings.py when ENVIRONMENT=test
# and ANTHIAS_TEST_DB_PATH is not set. The
# `-journal` / `-wal` / `-shm` siblings appear in WAL mode.
.anthias-test.db
.anthias-test.db-journal
.anthias-test.db-wal
.anthias-test.db-shm

# Local agent state (Claude Code)
.claude/

# Django
staticfiles/

# Hugo
website/public/
website/resources/
website/.hugo_build.lock

# ESLint
.eslintcache

# Linting
.coffeelint.json
