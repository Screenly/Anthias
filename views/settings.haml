!!! 5
%html

  %head
    %meta(charset="utf-8")
    %title Screenly OSE

    %link(href="/static/css/bootstrap.min.css", rel="stylesheet")
    %link(href="/static/css/screenly.css", rel="stylesheet")
    %link(href="/static/css/toggle-switch.css", rel="stylesheet")
    %script(src="/static/js/jquery-1.9.1.min.js")

  %body

    .navbar.navbar-fixed-top
      .navbar-inner
        .container
          %a.brand(href="/")
            %strong Screenly
            %span OSE

          %ul.nav.pull-right
            - if not up_to_date:
              %li.update-available
                %a(href="#")
                  %i.icon-circle-arrow-down.icon-white
                  Update Available
            %li
              %a(href="/settings")
                %i.icon-cog.icon-white
                Settings
            %li.divider-vertical
            %li
              %a(href="/system_info")
                %i.icon-tasks.icon-white
                System Info

    .container
      .row
        .span12
          %h1.page-header
            Settings


          - if flash:
            <div class="alert alert-${flash['class']}">
              = flash['message']
            </div>

          %form.form-horizontal(method="post")
            .control-group
              %label.control-label Show Splash Screen
              .controls

                %label.checkbox.toggle.well#splash_checkbox
                  - if show_splash == 'True':
                    %input#id(type="checkbox", checked="checked", name="show_splash")
                  - else:
                    %input#id(type="checkbox", name="show_splash")
                  %p
                    %span.on On
                    %span.off Off
                  %a.btn.btn-primary.slide-button

                .help-text
                  Specifies whether the Screenly OSE splash screen should be shown.

            .control-group
              %label.control-label Audio Out
              .controls
                %select(name="audio_output")
                  - if audio_output == 'hdmi':
                    %option(value="hdmi", selected="selected") HDMI
                    %option(value="local") 3.5mm jack
                  - else:
                    %option(value="hdmi") HDMI
                    %option(value="local", selected="selected") 3.5mm jack
                .help-text
                  Specify where audio from this node should be sent.

            .control-group
              %label.control-label Playlist Shuffling
              .controls

                %label.checkbox.toggle.well#shuffle_checkbox
                  - if shuffle_playlist == 'True':
                    %input#id(type="checkbox", checked="checked", name="shuffle_playlist")
                  - else:
                    %input#id(type="checkbox", name="shuffle_playlist")
                  %p
                    %span.on On
                    %span.off Off
                  %a.btn.btn-primary.slide-button

                .help-text
                  Toggle the shuffling of the playlist when being displayed through the viewer.

            .form-actions
              %input.btn.btn-primary(type="submit", value="Save Settings")
              %a.btn(href="/") Cancel

    #footer
      .container
        .copy
          &copy; <strong>2012-2013.</strong> Wireload, Inc.
        %a(id="wireload-logo", href="http://wireload.net/")
          %img(src="/static/img/wireload-logo.png", alt="Wireload")
