- name: Install Screenly
  hosts: all
  user: pi
  become: yes
  vars:
    manage_network: "{{ lookup('env', 'MANAGE_NETWORK') }}"

  handlers:
    - name: restart-nginx
      service:
        name: nginx
        state: restarted

  roles:
    - system
    - rpi-update
    - screenly
    - network
    - splashscreen
    - nginx
    - ssl
