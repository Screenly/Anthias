{{ anthias_user }} ALL=NOPASSWD: /usr/local/sbin/upgrade_anthias.sh

# Restricted to the specific systemctl verbs host_agent.py invokes.
# /bin/systemctl with no arg restriction would let the user start/stop/mask
# any unit on the system.
{{ anthias_user }} ALL=NOPASSWD: /sbin/shutdown, /bin/systemctl reboot, /bin/systemctl poweroff

#External
{{ anthias_user }} ALL=NOPASSWD: /usr/local/sbin/wifi-connect
