# Anthias silent boot — included from config.txt.
#
# Pi firmware processes `include` inline, inheriting the enclosing section
# filter. The leading [all] guarantees our directives apply on every model
# regardless of which section the `include` line lands in.
[all]

# Suppress the firmware rainbow splash screen.
disable_splash=1
